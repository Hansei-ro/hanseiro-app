# 개발 컨벤션 (상세 가이드)

> 💡 **개발 컨벤션이란?** 팀원들이 같은 방식으로 코드를 작성하고 관리하기 위한 규칙입니다. 처음에는 복잡해 보이지만, 이 규칙을 따르면 나중에 코드를 찾고 수정하기가 훨씬 쉬워집니다!

---

## 📌 Git 기본 개념 (처음이라면 꼭 읽어보세요!)

### Git이란?

**Git**은 코드의 변경 이력을 저장하는 도구입니다. 여러 사람이 동시에 작업해도 충돌 없이 협업할 수 있게 도와줍니다.

### GitHub이란?

**GitHub**은 Git으로 관리하는 코드를 온라인에 저장하는 서비스입니다. 팀원들과 코드를 공유하고 함께 작업할 수 있습니다.

### 기본 용어 정리

| **용어**                | **설명**                                | **비유**                           |
| ----------------------- | --------------------------------------- | ---------------------------------- |
| **Repository (저장소)** | 프로젝트 코드가 저장되는 공간           | 팀의 공동 작업 폴더                |
| **Branch (브랜치)**     | 독립적으로 작업할 수 있는 작업 공간     | 원본을 복사해서 따로 작업하는 공간 |
| **Commit (커밋)**       | 변경사항을 저장하는 것                  | 작업한 내용을 "저장"하는 것        |
| **Push (푸시)**         | 로컬의 커밋을 GitHub에 올리는 것        | 내 컴퓨터 → 클라우드로 업로드      |
| **Pull (풀)**           | GitHub의 코드를 내 컴퓨터로 가져오는 것 | 클라우드 → 내 컴퓨터로 다운로드    |
| **Merge (머지)**        | 두 브랜치를 합치는 것                   | 두 개의 작업을 하나로 합치기       |
|                         |                                         |                                    |

---

## 🌿 브랜치 전략 (Branch Strategy)

> 💡 **브랜치란?** 원본 코드를 건드리지 않고 안전하게 작업할 수 있는 독립된 작업 공간입니다.

### 브랜치 구조

```jsx
main (메인 브랜치 - 실제 배포되는 코드)
  ├── develop (개발 브랜치 - 개발 중인 코드를 모아두는 곳)
       ├── feature/login (기능 개발)
       ├── feature/matching-algorithm
       ├── fix/login-error (버그 수정)
       ├── style/button-color (디자인 수정)
       └── hotfix/critical-bug (긴급 수정)
```

### 브랜치 종류와 역할

| **브랜치**                   | **역할**                       | **누가 작업?**  | **예시**               |
| ---------------------------- | ------------------------------ | --------------- | ---------------------- |
| **main**                     | 실제 서비스에 배포되는 코드    |
| ❗절대 직접 수정 금지        | 관리자만                       | -               |
| **develop**                  | 개발 중인 코드를 모아두는 곳   |
| 모든 기능 개발은 여기서 시작 | 모든 팀원                      | -               |
| **feature/**                 | 새로운 기능을 개발할 때        | 개발자 각자     | `feature/login`        |
| `feature/chat`               |
| **fix/**                     | 버그를 수정할 때               | 개발자 각자     | `fix/login-error`      |
| `fix/matching-bug`           |
| **style/**                   | UI 디자인을 수정할 때          | 디자이너/개발자 | `style/button-color`   |
| `style/font-size`            |
| **hotfix/**                  | 배포된 서비스의 긴급 버그 수정 | 관리자/담당자   | `hotfix/payment-error` |

---

### 브랜치 이름 짓기 규칙

**형식:** `타입/간단한-설명`

✅ **좋은 예시:**

- `feature/user-login` → 사용자 로그인 기능
- `feature/matching-request` → 매칭 요청 기능
- `fix/signup-validation` → 회원가입 검증 버그 수정
- `style/login-button` → 로그인 버튼 스타일 수정

❌ **나쁜 예시:**

- `feature/기능1` → 한글 사용 금지
- `fix-bug` → 타입 뒤에 `/` 없음
- `feature/user_login` → 언더바(`_`) 대신 하이픈(`-`) 사용
- `mywork` → 어떤 작업인지 알 수 없음

---

### 브랜치 작업 흐름 (실전 가이드)

### 1️⃣ 새로운 기능 개발 시작하기

```bash
# 1. develop 브랜치로 이동
git checkout develop

# 2. 최신 코드 받아오기 (중요!)
git pull origin develop

# 3. 새 브랜치 생성하고 이동
git checkout -b feature/login

# 이제 이 브랜치에서 코드 작업하면 됩니다!
```

### 2️⃣ 작업한 내용 GitHub에 올리기

```bash
# 1. 변경된 파일 확인
git status

# 2. 변경된 파일 추가 (모든 파일)
git add .

# 3. 커밋 (저장) - 메시지는 아래 커밋 컨벤션 참고
git commit -m "feat(auth): 로그인 UI 구현"

# 4. GitHub에 올리기
git push origin feature/login
```

### 3️⃣ Pull Request 만들기 (웹에서 진행)

1. GitHub 저장소 페이지로 이동
2. "Compare & pull request" 버튼 클릭
3. PR 제목과 내용 작성 (아래 PR 컨벤션 참고)
4. 리뷰어 지정 (팀원 중 코드를 확인해줄 사람)
5. "Create pull request" 클릭

### 4️⃣ 코드 리뷰 받고 수정하기

```bash
# 리뷰어가 수정 요청을 했다면
# 같은 브랜치에서 계속 작업 후 다시 push

git add .
git commit -m "fix(auth): 리뷰 반영 - 비밀번호 검증 로직 수정"
git push origin feature/login

# Push하면 PR에 자동으로 반영됩니다!
```

### 5️⃣ 승인 받으면 Merge (병합)

- 리뷰어가 승인하면 "Merge pull request" 버튼 클릭
- Merge 후 브랜치는 자동으로 삭제됩니다

### 6️⃣ 다음 작업 시작하기

```bash
# 1. develop 브랜치로 돌아가기
git checkout develop

# 2. 최신 코드 받아오기 (내가 merge한 코드 포함)
git pull origin develop

# 3. 새로운 기능 브랜치 생성
git checkout -b feature/다음기능

# 다시 1번부터 반복!
```

---

### ⚠️ 자주하는 실수와 해결법

### 실수 1: "내 코드가 다른 사람 코드랑 충돌나요!"

**원인:** develop을 최신으로 받지 않고 작업 시작

**해결:**

```bash
git checkout develop
git pull origin develop
git checkout feature/내브랜치
git merge develop  # develop의 최신 코드를 내 브랜치로 가져오기
```

### 실수 2: "실수로 develop에서 직접 작업했어요!"

**해결:**

```bash
# 아직 커밋 안 했다면
git stash  # 변경사항 임시 저장
git checkout -b feature/새브랜치
git stash pop  # 저장한 변경사항 복구
```

### 실수 3: "브랜치를 잘못 만들었어요!"

**해결:**

```bash
# 브랜치 이름 변경
git branch -m 잘못된이름 올바른이름

# 또는 브랜치 삭제하고 다시 만들기
git checkout develop
git branch -D 잘못된브랜치
git checkout -b 올바른브랜치
```

---

## 💬 커밋 컨벤션 (Commit Convention)

> 💡 **커밋 메시지란?** 내가 어떤 작업을 했는지 기록하는 메모입니다. 나중에 "이 버그가 언제 생겼지?" 찾을 때 아주 중요합니다!

### 커밋 메시지 기본 형식

```jsx
<타입>(<범위>): <제목>

<본문> (선택사항)

<꼬리말> (선택사항)
```

**실제 예시:**

```jsx
feat(auth): 로그인 페이지 UI 구현

- 이메일 입력 필드 추가
- 비밀번호 입력 필드 추가
- 로그인 버튼 추가

Resolves: #12
```

---

### 타입 (Type) - 어떤 종류의 작업인가?

| **타입**        | **의미**                  | **사용 예시**                 |
| --------------- | ------------------------- | ----------------------------- |
| `feat`          | ✨ 새로운 기능 추가       | 로그인 기능, 매칭 알고리즘 등 |
| `fix`           | 🐛 버그 수정              | 로그인 안 되는 문제 해결      |
| `style`         | 💄 코드 외관 수정         |
| (동작은 그대로) | 버튼 색상 변경, 여백 조정 |
| `refactor`      | ♻️ 코드 개선              |
| (기능은 그대로) | 함수 이름 변경, 코드 정리 |
| `docs`          | 📝 문서 수정              | README 작성, 주석 추가        |
| `test`          | ✅ 테스트 코드 추가       | 로그인 테스트 코드 작성       |
| `chore`         | 🔧 기타 작업              | 패키지 설치, 설정 파일 수정   |
| `remove`        | 🔥 코드/파일 삭제         | 사용하지 않는 컴포넌트 삭제   |

---

### 범위 (Scope) - 어느 부분을 수정했나? (선택사항)

**예시:**

- `auth` → 인증 관련
- `match` → 매칭 관련
- `chat` → 채팅 관련
- `ui` → UI 관련
- `api` → API 관련

---

### 제목 (Subject) - 무엇을 했나?

**규칙:**

1. **50자 이내**로 간결하게
2. **마침표(`.`) 금지**
3. **명령문**으로 작성 ("추가했다" ❌ → "추가" ⭕)

✅ **좋은 예시:**

- `feat(auth): 로그인 페이지 UI 구현`
- `fix(match): 매칭 시간 계산 오류 수정`
- `style(button): 로그인 버튼 색상 변경`
- `docs(readme): 프로젝트 설치 방법 추가`

❌ **나쁜 예시:**

- `로그인 기능 추가했음.` → 타입 없음, 마침표 있음
- `feat: 기능 추가` → 어떤 기능인지 알 수 없음
- `fix(match): 버그를 수정했습니다.` → 명령문 아님, 마침표 있음
- `asdfasdf` → 의미 없는 메시지

---

### 본문 (Body) - 자세한 설명 (선택사항)

**언제 쓰나요?**

- 변경 사항이 복잡할 때
- 왜 이렇게 수정했는지 설명하고 싶을 때

**작성 방법:**

- 제목 다음에 **빈 줄 한 줄** 추가 후 작성
- **어떻게**보다 **왜**를 설명
- 여러 줄로 작성 가능

**예시:**

```jsx
feat(auth): JWT 토큰 인증 방식 적용

세션 방식에서 JWT 방식으로 변경했습니다.
이유:
- 서버 메모리 부담 감소
- 모바일 앱과의 통합이 쉬움
- 확장성 향상
```

---

### 꼬리말 (Footer) - 이슈 연결 (선택사항)

**형식:**

```jsx
Resolves: #이슈번호  (이슈 해결 완료)
Ref: #이슈번호       (이슈 참고)
Related to: #이슈번호 (이슈 관련)
```

**예시:**

```jsx
feat(auth): 로그인 기능 구현

Resolves: #12
```

---

### 커밋 메시지 전체 예시

### 예시 1: 간단한 커밋

```jsx
feat(auth): 회원가입 이메일 검증 추가
```

### 예시 2: 자세한 커밋

```jsx
fix(match): 매칭 알고리즘 시간 계산 오류 수정

기존에는 ±15분 범위를 계산할 때 시간대를 고려하지 않아
자정을 넘어가는 경우 오류가 발생했습니다.

수정 내용:
- 시간 계산 시 날짜 변경 고려
- 테스트 케이스 추가

Resolves: #45
```

### 예시 3: 여러 파일 수정

```jsx
style(ui): 전체 버튼 디자인 통일

- Primary 버튼: 파란색 배경
- Secondary 버튼: 흰색 배경 + 파란색 테두리
- 모든 버튼에 hover 효과 추가

Related to: #23
```

---

### 💡 커밋 꿀팁

### 1. 작은 단위로 자주 커밋하기

❌ **나쁜 예:** 하루 종일 작업하고 한 번에 커밋

```jsx
feat(all): 로그인, 회원가입, 매칭, 채팅 모두 구현
```

⭕ **좋은 예:** 기능 하나씩 완성될 때마다 커밋

```jsx
feat(auth): 로그인 UI 구현
feat(auth): 로그인 API 연동
feat(auth): 로그인 에러 처리 추가
```

### 2. 테스트 후 커밋하기

- 커밋 전에 꼭 빌드 테스트 진행 후 코드가 작동하는지 확인! (빌드 테스트도 진행하기!)
- 에러가 있는 코드를 커밋하면 팀원들이 곤란해집니다

### 3. 커밋 메시지 수정하기 (직전 커밋만 가능)

```bash
# 커밋 메시지 오타 수정
git commit --amend -m "올바른 메시지"
```

---

## 🎫 이슈 컨벤션 (Issue Convention)

> 💡 **이슈란?** 해야 할 작업, 버그, 개선 사항 등을 기록하는 할 일 목록입니다. GitHub Issues에서 관리합니다.

### 이슈 제목 형식

```jsx
[타입] 간결하고 명확한 제목
```

---

### 이슈 타입

| **타입**     | **설명**         | **예시**                              |
| ------------ | ---------------- | ------------------------------------- |
| `[feature]`  | 새로운 기능 개발 | `[feature] 로그인 기능 구현`          |
| `[bug]`      | 버그 수정        | `[bug] 로그인 시 에러 발생`           |
| `[style]`    | 디자인 작업      | `[style] 메인 화면 UI 디자인`         |
| `[docs]`     | 문서 작업        | `[docs] API 명세서 작성`              |
| `[refactor]` | 코드 개선        | `[refactor] 매칭 알고리즘 최적화`     |
| `[question]` | 질문/논의        | `[question] 인증 방식 어떻게 할까요?` |

---

### 이슈 내용 템플릿

### 기능 개발 이슈 템플릿

```markdown
## 📝 개요 (Summary)

-

## ✅ 할 일 목록 (To-Do)

- [ ]
- [ ]

## 🎯 완료 조건 (Acceptance Criteria)

-

## 📎 참고 자료 (References)

-
```

---

### 이슈 라벨 (Label) 사용하기

GitHub에서 이슈에 라벨을 붙여 분류할 수 있습니다.

| **라벨**           | **색상** | **설명**                |
| ------------------ | -------- | ----------------------- |
| `bug`              | 🔴 빨강  | 버그 수정               |
| `feature`          | 🟢 초록  | 새 기능                 |
| `style`            | 🟣 보라  | 디자인 작업             |
| `documentation`    | 🔵 파랑  | 문서 작업               |
| `Priority: High`   | 🔴 빨강  | 긴급한 작업             |
| `Priority: Medium` | 🟡 노랑  | 보통 작업               |
| `Priority: Low`    | 🟢 초록  | 천천히 해도 되는 작업   |
| `Good First Issue` | 🟢 초록  | 처음 시작하기 좋은 작업 |

---

### 이슈 작성 실전 가이드

### 1️⃣ 이슈 생성하기 (GitHub 웹에서)

1. GitHub 저장소 → **Issues 탭** 클릭
2. **New issue** 버튼 클릭
3. 제목과 내용 작성
4. 라벨 선택
5. 담당자 지정 (Assignees)
6. **Submit new issue** 클릭

### 2️⃣ 이슈 번호 활용하기

- 이슈를 만들면 자동으로 번호가 부여됩니다 (예: #12)
- 브랜치 이름에 이슈 번호 포함하기:

```bash
git checkout -b feature/login-#12
```

- 커밋 메시지에 이슈 번호 포함하기:

```jsx
feat(auth): 로그인 UI 구현

Ref: #12
```

### 3️⃣ 이슈 닫기

**자동으로 닫기:** PR이 merge되면 자동으로 닫힘

```jsx
feat(auth): 로그인 기능 완료

Resolves: #12
```

**수동으로 닫기:** GitHub 이슈 페이지에서 "Close issue" 버튼 클릭

---

## 🔀 Pull Request (PR) 컨벤션

> 💡 **Pull Request란?** "내가 작성한 코드를 develop 브랜치에 합쳐주세요!"라고 요청하는 것입니다. 팀원들이 코드를 검토(리뷰)한 후 승인하면 합쳐집니다.

### PR 제목 형식

**커밋 메시지와 동일한 형식 사용:**

```jsx
<타입>(<범위>): <제목>
```

**예시:**

```jsx
feat(auth): 로그인 기능 구현
fix(match): 매칭 알고리즘 오류 수정
style(button): 버튼 색상 변경
```

---

### PR 본문 템플릿

```markdown
## 🔗 관련 이슈

Resolves: #12

## 📝 작업 내용

무엇을 했는지 간단히 설명해주세요.

-

## 📷 스크린샷 (UI 작업인 경우)

작업 전과 후를 비교해주세요.

**Before:**
[이미지]

**After:**
[이미지]

## 💬 리뷰어에게

특별히 확인해주셨으면 하는 부분이 있다면 적어주세요.

예: "비밀번호 검증 로직이 맞는지 확인 부탁드립니다."

## ✅ 체크리스트

- [ ] 코드가 정상적으로 실행되나요? (빌드 에러 없음)
- [ ] 불필요한 주석이나 `console.log`를 제거했나요?
- [ ] `Prettier`를 통해 코드 스타일을 정렬했나요?
```

---

### PR 생성 실전 가이드

### 1️⃣ PR 만들기 (GitHub 웹에서)

1. 브랜치를 push한 후 GitHub 저장소 페이지 이동
2. **"Compare & pull request"** 버튼 클릭 (자동으로 나타남)
3. **Base 브랜치**: `develop` 선택 (여기로 합쳐짐)
4. **Compare 브랜치**: 내가 작업한 브랜치 (자동 선택됨)
5. PR 제목과 본문 작성
6. **Reviewers**: 리뷰해줄 팀원 선택
7. **Assignees**: 나 자신 선택
8. **Labels**: 적절한 라벨 선택
9. **Create pull request** 클릭

### 2️⃣ 코드 리뷰 받기

- 리뷰어가 코드를 확인하고 **Comment** 남김
- **Request changes**: 수정 요청 → 수정 후 다시 push
- **Approve**: 승인 → Merge 가능!

### 3️⃣ 리뷰 피드백 반영하기

```bash
# 같은 브랜치에서 계속 작업
git add .
git commit -m "fix(auth): 리뷰 반영 - 비밀번호 검증 추가"
git push origin feature/login

# PR에 자동으로 새 커밋이 추가됩니다!
```

### 4️⃣ Merge 하기

**리뷰어가 승인하면:**

1. **Merge pull request** 버튼 클릭
2. Merge 방식 선택:
   - **Merge commit** (추천): 모든 커밋 이력 유지
   - **Squash and merge**: 여러 커밋을 하나로 합침
   - **Rebase and merge**: 커밋 이력을 일렬로 정리
3. **Confirm merge** 클릭
4. **Delete branch** 클릭 (브랜치 삭제)

---

### PR 리뷰 체크리스트

### 리뷰 요청자 (PR 작성자)

- [ ] 코드가 정상 작동하는지 로컬에서 테스트
- [ ] 불필요한 파일이 포함되지 않았는지 확인 (`console.log` 제거 등)
- [ ] 커밋 메시지가 컨벤션을 따르는지 확인
- [ ] PR 본문에 변경 사항 명확히 작성
- [ ] 관련 이슈 번호 연결
- [ ] 스크린샷 첨부 (UI 작업인 경우)

### 리뷰어 (코드 검토자)

- [ ] 코드가 요구사항을 충족하는가?
- [ ] 버그가 없는가?
- [ ] 코드가 읽기 쉬운가?
- [ ] 보안 문제는 없는가?
- [ ] 성능 문제는 없는가?
- [ ] 테스트가 필요한 경우 테스트 코드가 있는가?

---

### PR 리뷰 코멘트 예시

### 좋은 코멘트 예시

⭕ **구체적이고 건설적:**

```jsx
✅ "이 부분은 `if-else` 대신 `switch`문을 사용하면 더 읽기 쉬울 것 같아요!"

✅ "비밀번호 검증 로직이 잘 구현되었네요.
   다만 특수문자 검사도 추가하면 보안이 더 강화될 것 같습니다."

✅ "UI가 깔끔하네요! 다만 모바일에서는 버튼이 작을 수 있으니
   미디어 쿼리로 사이즈 조정하면 좋을 것 같아요."
```

### 나쁜 코멘트 예시

❌ **모호하거나 부정적:**

```jsx
❌ "이상해요."
❌ "이렇게 하면 안 돼요."
❌ "왜 이렇게 했어요?"
```

---

### 💡 PR 꿀팁

### 1. Draft PR 활용하기

- 작업 중인 코드를 미리 공유하고 싶을 때
- PR 생성 시 **"Create draft pull request"** 선택
- 완료되면 **"Ready for review"** 클릭

### 2. PR을 작은 단위로 나누기

- ❌ 나쁜 예: 한 PR에 10개 기능 몽땅
- ⭕ 좋은 예: 기능 하나씩 PR 생성

### 3. 리뷰 요청 전 셀프 리뷰

- GitHub에서 **Files changed** 탭에서 내 코드 다시 확인
- 불필요한 코드나 주석 제거

---

## 📚 빠른 참고 가이드 (Cheat Sheet)

### 브랜치 명령어

```bash
# 현재 브랜치 확인
git branch

# 새 브랜치 생성
git branch feature/login

# 브랜치 이동
git checkout feature/login

# 브랜치 생성 + 이동 (한 번에)
git checkout -b feature/login

# 브랜치 삭제
git branch -d feature/login

# 원격 브랜치 목록 확인
git branch -r
```

### 커밋 명령어

```bash
# 변경 사항 확인
git status

# 모든 파일 추가
git add .

# 특정 파일만 추가
git add src/Login.js

# 커밋
git commit -m "feat(auth): 로그인 기능 추가"

# 커밋 메시지 수정 (직전 커밋만)
git commit --amend -m "수정된 메시지"

# 커밋 이력 확인
git log
git log --oneline  # 한 줄로 보기
```

### 동기화 명령어

```bash
# 원격 저장소에서 최신 코드 받아오기
git pull origin develop

# 로컬 커밋을 원격 저장소에 올리기
git push origin feature/login

# 원격 브랜치 목록 업데이트
git fetch
```

### 자주 쓰는 조합

```bash
# 새 작업 시작
git checkout develop
git pull origin develop
git checkout -b feature/새기능

# 작업 후 저장
git add .
git commit -m "feat: 작업 내용"
git push origin feature/새기능

# 다음 작업 준비
git checkout develop
git pull origin develop
```

---

## ❓ 자주 묻는 질문 (FAQ)

### Q1. develop과 main의 차이가 뭔가요?

**A:**

- `develop`: 개발 중인 코드 (아직 테스트 중)
- `main`: 실제 서비스에 배포된 코드 (안정적)

개발은 항상 `develop`에서 하고, 테스트가 완료되면 `main`에 배포합니다.

---

### Q2. 커밋을 여러 개 해야 하나요, 한 번에 해야 하나요?

**A:** 의미 있는 단위로 자주 커밋하세요!

⭕ 좋은 예:

```jsx
feat(auth): 로그인 UI 추가
feat(auth): 로그인 API 연동
feat(auth): 에러 처리 추가
```

❌ 나쁜 예:

```jsx
feat(auth): 로그인 관련 모든 작업 완료 (100개 파일 수정)
```

---

### Q3. 실수로 잘못된 브랜치에서 작업했어요!

**A:** 아직 커밋하지 않았다면:

```bash
git stash              # 변경사항 임시 저장
git checkout develop   # 올바른 브랜치로 이동
git checkout -b feature/올바른브랜치
git stash pop         # 저장한 변경사항 복구
```

---

### Q4. Pull Request를 만들었는데 수정이 필요해요!

**A:** 같은 브랜치에서 계속 작업 후 push하면 자동으로 PR에 반영됩니다.

```bash
git add .
git commit -m "fix: 리뷰 반영"
git push origin feature/login
```

---

### Q5. Merge 충돌이 발생했어요!

**A:** 당황하지 마세요! 다른 사람과 같은 파일을 수정했을 때 발생합니다.

**해결 방법:**

```bash
git pull origin develop  # 최신 코드 받기

# VS Code에서 충돌 난 파일 열기
# "Accept Current Change" 또는 "Accept Incoming Change" 선택

git add .
git commit -m "chore: merge 충돌 해결"
git push origin feature/login
```

---

### Q6. 커밋 메시지를 영어로 써야 하나요?

**A:** 팀 내에서 통일하면 됩니다!

- 영어: `feat(auth): add login feature`
- 한글: `feat(auth): 로그인 기능 추가`

**이 프로젝트에서는 한글을 사용하기로 했습니다!**

---

## 🎯 컨벤션 준수 체크리스트

작업 전에 이 체크리스트를 확인하세요!

### 브랜치 작성 시

- [ ] `develop`에서 최신 코드를 받았나요?
- [ ] 브랜치 이름이 `타입/설명` 형식인가요?
- [ ] 브랜치 이름에 한글이 없나요?

### 커밋 작성 시

- [ ] 커밋 타입이 명확한가요? (`feat`, `fix`, `style` 등)
- [ ] 제목이 50자 이내인가요?
- [ ] 제목 끝에 마침표(`.`)가 없나요?
- [ ] 무엇을 했는지 명확하게 작성했나요?

### PR 생성 시

- [ ] PR 제목이 커밋 컨벤션을 따르나요?
- [ ] PR 본문에 작업 내용을 명확히 작성했나요?
- [ ] 관련 이슈 번호를 연결했나요?
- [ ] 리뷰어를 지정했나요?
- [ ] 로컬에서 테스트했나요?
- [ ] 불필요한 파일이 포함되지 않았나요?

---

## 🆘 도움이 필요할 때

- **Git 사용법**: 팀원에게 물어보기, Google 검색
- **충돌 해결**: 먼저 시도해보고 안 되면 팀원에게 도움 요청
- **컨벤션 관련**: 이 문서를 참고하거나 팀 채팅방에 질문하기

**💡 기억하세요:** 처음에는 누구나 어렵습니다. 실수해도 괜찮아요! 중요한 건 계속 시도하고 배우는 것입니다. 💪
